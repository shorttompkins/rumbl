<h1>Listing Users</h1>

<table class="table">
  <!--
  Note use of @var for variables provided by the VIEW versus just normal var which are declared
  within the context of this HTML file (i.e. @users versus user)
  -->
  <%= for user <- @users do %>
    <tr>
      <td><%= render "user.html", user: user %></td>
      <td><%= link "View", to: user_path(@conn, :show, user.id) %></td>
    </tr>
  <% end %>
</table>
